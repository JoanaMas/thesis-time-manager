import { FC, ReactElement }from 'react';
// Style
import './illustrationSection.module.scss';

const IllustrationSection: FC = (): ReactElement => {
  return (
    <div className='illustration'>Illustration</div>
  );
};

export default IllustrationSection;